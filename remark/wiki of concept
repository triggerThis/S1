jquery
	闭包
	函数形成闭包的机制
	闭包是指你的具体的一个方法定义, 当调用时, 此方法会成为对象, 一个对象的生命期内, 闭包变量是给扔在内存中了
	设foo(){var m=1, fsub(){++m}}, var c1, c2, c3 = foo; 在c1(), c2, c3序列下,m如何变化;
		c3, c2. 
	m 对外部是不可直接访问的, 有点类似于set 
	2.this 绑定上下文
	3.let
	4.不用var而随意写的变量, 作用域是全局的
	初级的前端可能不会用到上面的概念
//--